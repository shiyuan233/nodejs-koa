doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(content="width=device-width, initial-scale=1.0" name="viewport")
        meta(content="ie=edge" http-equiv="X-UA-Compatible")
        title Document
        link(
            href="/admin/addNews.css"
            rel="stylesheet"
            type="text/css"
        )
    body
        .menu-content
            h1 添加新闻
            form.addInput(action="/admin/addData" method="post" enctype="multipart/form-data")
                div
                    | 标题：
                    input.title(type="text" name='title')
                .contentContainer
                    | 内容：
                    textarea.content(
                        cols="30"
                        id=""
                        name="content"
                        rows="10"
                    )
                div
                    | 类型：
                    select.selectStyle(id="" name="type")
                        option(value="逮捕") 逮捕
                        option(value="消息") 消息
                        option(value="通讯") 通讯
                        option(value="文学") 文学
                .uploadContainer
                    | 缩略图：
                    span.fileinput-button
                        span 上传图片
                        input.imgFile(name="img" type="file" accept='image/png,image/jpg')
                div
                    button.btn(type="submit") 提交